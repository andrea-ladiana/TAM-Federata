# Changelog

## Unreleased

- Reorganized to `src/` layout and created `unsup` package
- Moved `stress_tests/` to `experiments/` and updated imports
- Moved `split_unsup.ipynb` to `notebooks/`
- Archived `risultati/` into `results/archive_risultati/`
- Cleaned caches and generated outputs; added `.gitignore`
- Added `README.md`, `requirements.txt`, `.editorconfig`, and docs
